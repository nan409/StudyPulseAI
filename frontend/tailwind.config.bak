/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {
      colors: {
        background: '#0a0a0f',
        surface: 'rgba(255, 255, 255, 0.03)',
        primary: '#38bdf8', /* Neon blue */
        secondary: '#a855f7', /* Purple */
        accent: '#ec4899', /* Pink pulse */
        text: '#f8fafc',
        muted: '#94a3b8'
      },
      backgroundImage: {
        'gradient-pulse': 'linear-gradient(135deg, #38bdf8 0%, #a855f7 50%, #ec4899 100%)',
      },
      animation: {
        'spin-slow': 'spin 3s linear infinite',
        'pulse-glow': 'pulse-glow 2s cubic-bezier(0.4, 0, 0.6, 1) infinite',
      },
      keyframes: {
        'pulse-glow': {
          '0%, 100%': { opacity: '1', filter: 'drop-shadow(0 0 10px rgba(56, 189, 248, 0.5))' },
          '50%': { opacity: '0.8', filter: 'drop-shadow(0 0 20px rgba(168, 85, 247, 0.8))' },
        }
      }
    },
  },
  plugins: [],
}
